#!/bin/bash

# Display all processes from all users, including those without a TTY
# Show user, PID, %CPU, %MEM, and command
# Display process hierarchy using indentation (forest)

ps -e -o user,pid,%cpu,%mem,cmd --forest
